iapiVersion: v1
kind: Service
metadata:
  name: nginx
  namespace: stevenlab
spec:
  type: NodePort
  selector:
    app: nginx
  ports:
    - port: 80          # 서비스 내부 포트
      targetPort: 80    # 파드의 컨테이너 포트
      nodePort: 30080   # 외부에서 접속할 포트 (선택, 없으면 자동 할당)
